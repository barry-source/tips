# 2026-07-01 Work Log

### 补充 Mach-O 文件结构到性能优化文档
- 根据截图中的思维导图，在 `Base/优化/Readme.md` 的启动优化章节中新增了 Mach-O 文件结构小节
- 补充内容：
  - 1.2.1 文件类型（file type）：Executable、dylib、Bundle、Image、Framework 的定义和对应关系
  - 1.2.2 Mach-O 文件结构：__TEXT、__DATA、__LINKEDIT 三个 Segment 的详细说明和与 dyld 启动的关系
  - 增加了 ASCII 结构图展示 Mach-O 文件布局
  - 整理了 Segment 属性、内容和说明的对比表格
- 修复了因新增章节导致的编号重复问题（1.3 main() 执行后优化 → 1.4 main() 执行后优化）

### 补充合并动态库的实战方案
- 在 `Base/优化/Readme.md` 的启动优化部分新增了"合并同类动态库的实战方案"小节
- 覆盖 6 种方案：CocoaPods 静态化（`:linkage => :static`）、聚合动态 Framework 脚本、混编方案、SPM 替代、手动合并 .a、仅编译必要架构
- 特别针对"第三方库不是自己写的"场景，给出具体 Podfile 配置和 bash 脚本示例
- 补充了方案对比表（适合场景、优缺点）
- 补充了 dyld 全称（dynamic link editor）到文档中

### 补充离屏渲染分类（渲染分类树）
- 在 `Base/优化/Readme.md` 的 2.4 离屏渲染详解中新增了 2.4.1 渲染分类
- 补充渲染分类树：CPU 渲染 vs GPU 渲染 → GPU 帧缓冲区渲染 vs 非帧缓冲区渲染
- 定义：CPU 渲染 + GPU 非帧缓冲区渲染 = 离屏渲染
- 增加了详细的对比表（处理单元、缓冲区位置、触发条件、是否可异步、性能影响）

### 创建 GATT 蓝牙文档
- 创建了 `Base/蓝牙/BLE-GATT.md` 文档，涵盖：
  - GATT 基本概念（协议栈分层、GATT Client/Server 角色）
  - GATT Profile 层次结构（Profile → Service → Characteristic → Descriptor）
  - CoreBluetooth 与 GATT 的映射关系
  - 四种 GATT 数据操作（Read/Write/Notify/Indicate）及时序图
  - MTU 详解与分包处理
  - 完整连接与数据交互时序图
  - 常见 BLE Service/Characteristic UUID 速查表
  - GATT 面试题 8 道
  - 最佳实践总结
- 更新了 `Base/Readme.md`，新增"蓝牙"分类导航
